现在我已经有了完整的交叉核对表。让我将每一条发现映射到草稿的覆盖范围上，并完成 a-e 的评估。

以下是我的评审意见：

---

## (1) 逐条 a-e 判断

### (a) 是否漏掉 Critique 中 critical/serious 的问题 — **基本 OK，但漏 2 条 serious + 1 条半**

逐项核对 34 条 finding 的覆盖：

**Critical（共 14 条，标注覆盖情况）：**
- 维度1：#1 目标错位 → 草稿 §1+§3.1 ✅；#2 |T> 不自洽 → §3.1 ✅；#4 F=1/±1 退化 → §2.4+§3.3+§3.1 ✅
- 维度2：#7 U_t 展平训不动 → §2.2+§3.2 ✅；#8 per-expand O(|A|) 前传 → §2.3+§5（expand top-K）✅；#9 节点存 U_t 内存炸裂 → §3.2 ✅；#10 fidelity 模拟成本 → §3.2+§3.1（circuit_implements）✅
- 维度3：#14 稀疏奖励双重失败 → §3.3 ✅；#16 模拟策略缺失+叶估值浪费 → §3.6 ✅；#19 门级是错误抽象层 → §3.7 ✅
- 维度4：#21 ShortCircuit 重叠 → §3.4 ✅；#22 维度灾难是劣势非创新 → §3.2+§5 ✅
- 维度5：#28 网络作用在平行体候选 → §4+§5 ✅；#29 RL→ILP MIPStart 反哺 → §4+§5 ✅

**全部 14 条 critical 均有可执行修复。OK。**

**Serious（共 12 条，标注覆盖情况）：**
- #3 相位编码≈phase oracle → §3.1(b) ✅
- #5 U_t 与 onset 脱节 → §3.2 ✅
- #6 RL+MCTS 已被 SSHR-MCTS/Beam 实现 → §3.7+§7 ✅
- #11 候选枚举 vs 门序列深度爆炸 → §4（候选 O(10^4) vs O(10^7)）⚠ **覆盖较弱**：草稿把这点合并进了"动作换块"的论证，但没有单独说明 SSHR 的 609441 是"结构化候选含几何"，new-idea 的 112 是"原子动作无几何先验"这个关键区分。算是半覆盖。
- #12 |T> oracle vs 稀疏态制备混淆 → §3.1+§1 ✅
- #13 替代表示（|ψ_t⟩ / stabilizer / XOR-AND 图）→ **❌ 漏掉**。草稿 §3.2 只给了"onset 位掩码"一条路，完全没有讨论"只存 |ψ_t⟩=U_t|0⟩ 而非全 U_t（2048x 压缩）"或"stabilizer-rank"或"XOR-AND 图"这三个替代表示。这是 serious finding，且与 §5 网络输入设计直接相关——补丁不够完整。
- #15 步惩罚 −λ 未标定 + 空线路陷阱 → §2.4+§3.3+§3.8 ✅（通过 z=−Σblock_cost 天然消解）
- #17 PUCT Q 更新疑似 bug → §3.5 ✅
- #18 动作空间无对称性破缺/剪枝 → §5（top-K）+§4（GNN 剪枝）✅，但 **NPN 归一化只在 §5 数据扩增里提了一句，没有作为搜索空间剪枝手段**（critique #18 的推荐(1)是 NPN 等价类归一化搜索空间，不是仅扩增训练数据）。半覆盖。
- #23 门级丢失 2^m 几何压缩 → §3.7+§5 ✅
- #24 缺 SL 预训练 → §3.4+§5 ✅
- #25 |T> 编码与 F 未明确 → §3.1 ✅
- #30 复用 ai_sshr 数据 pipeline → §4+§5 ✅
- #31 gnn-sshr cand_h 作编码层 → §4+§5 ✅
- #32 策略网替代 beam top_k / MCTS rollout → §4+§5 ✅

**漏掉/弱覆盖：#13（替代表示全景）这条 serious 完全没覆盖；#11、#18 半覆盖。**

**结论：critical 全覆盖；serious 漏 1 条 + 半覆盖 2 条。判定 = 有缺陷（接近 OK，但补丁不完整）。**

### (b) 四大要点是否给可执行修复 — **全部 OK**

| 要点 | 草稿位置 | 是否可执行 |
|------|---------|-----------|
| **\|T> 定义** | §3.1 | ✅ 给了显式修复：改 bit-flip oracle `U_G\|x⟩\|0⟩=\|x⟩\|f(x)⟩`，验证器 `circuit_implements`（O(2^n)、F∈{0,1}），并逐一否决振幅/相位/稀疏三种解读。**这是真正的可执行修复**（有现成验证器路径 `tests/test_correctness.py`）。 |
| **U_t 表示爆炸** | §2.2+§3.2+§5 | ✅ 给了显式修复：弃用 U_t，状态=onset int 掩码（n=8 仅 32 字节），节点只存 `(A_mask, cost_so_far, path)`，Ut 按需重建（指向 `sshr_mcts_v2.py` 的真实做法）。给了量化对比（524KB→32 字节）。**可执行**。 |
| **稀疏奖励** | §2.4+§3.3+§3.6 | ✅ 给了两层修复：块级 MDP（reward=−Σblock_cost，复用 `sshr_mcts_v2._backpropagate`）+ AlphaZero 叶估值（z←V_φ(leaf)，跳过 rollout）+ SL bootstrap。**可执行**。 |
| **ShortCircuit 重叠** | §3.4+§5+§7 | ✅ 给了 3 支柱差异化（量子度量 + 平行体 macro-action + SL bootstrap）+ Delta 表逐方法对比。**可执行且诚实**（明确承认 new-idea"连 SL 都没有反而更弱"）。 |

### (c) 整合是否真"1+1>2"（非拼凑） — **OK，但有一个软肋**

**强证据（支持真整合）：**
- §4 整合表 11 行，每行都有"仓库组件→接到 new-idea 哪个组件→预期收益"的闭环映射，不是清单式罗列。
- "RL→ILP MIPStart 反哺"（§4 第5行 + §3.4）是**双向**通道（SSHR 喂 new-idea teacher / new-idea 喂 SSHR warm-start），这是真正的 1+1>2 标志——单方向的复用只是 1+1=2。
- §5 修正版架构是**自底向上的三层栈**（MCTS / 网络层 / block_synth），每层都点名复用哪个仓库模块，不是"把 SSHR 块当动作"这种单点嫁接。
- monotone 语义统一（§4 风险点 7 + §5）解决了 critique #34 的 XOR/monotone 标签冲突——这是整合时容易被忽视的细节，草稿抓住了。

**软肋（扣分项）：**
- §5 的"修正版"本质上是 **AI_SSHR_DESIGN.md 已有的"AI-Guided Beam"思路 + 加一个 value 网络做 learned pruning + 反哺 ILP**。草稿在 §7 末尾的 Delta 表里诚实承认"本版 = SSHR-MCTS/Beam + π_θ/V_φ"和"GNN-SSHR + RL/MCTS 内联"，但**没有论证这三个已有部件的组合为什么会产生超出三者之和的效果**。1+1>2 的论证停留在"动作空间压缩 + 学习先验 + ILP 反哺"三个独立收益的并列，缺少一个"为什么只有组合起来才能解锁 n≥7"的因果论证（例如：单 GNN-SSHR 剪枝无法处理 ILP 失效区、单 RL 无法处理候选爆炸、只有 RL+几何候选+ILP warm-start 三角才能闭环）。这是诚实但偏保守。

**判定：OK（双向反哺 + 三层栈是真整合；但 1+1>2 的因果论证偏弱，仅 1+1≥2）。**

### (d) 实验路线图是否有可证伪指标 + baseline — **OK**

**可证伪指标（明确 pass/fail 判据）：**
- Phase 1：n=4 修正版 ≥ SSHR-I 最优解的 95% **且** 优于 SSHR-MCTS v2 → 这是**硬性可证伪门槛**，达不到则设计被证伪。
- 各 Phase 都有量化指标：CNOT 数、T 数、深度、成功率(%)、wall-clock、ILP 加速比。

**Baseline 完备性：**
- Phase 0/1/2/3 baseline 覆盖：SSHR-H/I/Beam/MCTS-v2（项目内）+ ShortCircuit 复现（适配量子度量）+ XAG/XORAX + Circuit Transformer。
- n=4 用 `paper_data.py` 的 CNOT=4696 作锚点（可验证复现正确性）。

**风险/回退已写明**：Gurobi license、n=5 ILP teacher 50-70h、n=7 GNN 推理延迟、n=8 object-array fallback，且每条都有回退（NPN 子集 / numba uint64×4）。这是可执行而非空话。

**软肋（扣分项）：**
- Phase 2 的 n=7 关键实验（论文核心 claim）只写了"修正版 vs SSHR-Beam → 证明学习带来增益"，但**没有给出"增益"的可证伪阈值**（例如：CNOT 降低 ≥ X% 才算 claim 成立）。Phase 1 有 95% 门槛，Phase 2 没有，是路线图的不对称。
- ShortCircuit "复现"只说"适配量子度量"，没说复现保真度怎么验证（ShortCircuit 是经典 AIG，"适配量子度量"可能引入不可控偏差，使对比不公平）。

**判定：OK（Phase 1 有硬门槛、baseline 全面、风险有回退；但 Phase 2 缺增益阈值、ShortCircuit 复现保真度未定义，扣分）。**

### (e) 是否诚实（无夸大新意） — **OK，是这份草稿最强的优点**

**诚实证据：**
- §1 一句话定位**直接承认 new-idea 在解一个"自身不自洽"的问题**，没有为原方案辩护。
- §3.4 明确写"ShortCircuit 已完整占据 AlphaZero 范式，new-idea 连 SL 预训练都没有，反而更弱"——**主动承认比 prior art 更弱**，没有偷换概念。
- §5 重新定位为"几何先验引导的层级化 AlphaZero"，**没有声称这是全新范式**，而是明确写"既非 ShortCircuit、也非 SSHR-MCTS、也非 GNN-SSHR"——是空白增量，不是 paradigm shift。
- §8 "最后一句诚实评估"：**点名 4 个 critical-blocker，判定原文"不可直接实现、不可复现、过不了 2026 顶会 novelty 关"**——对原始 new-idea.pdf 的批判比 critique JSON 还狠。
- Delta 表（§7）逐方法列出 Delta，没有把"换了个域"包装成方法创新。

**唯一轻微不诚实处：**
- §5 标题"几何先验引导的层级化 AlphaZero"中的"层级化"——草稿的实际架构是**单层** MCTS（动作=块），block_synth 是确定性投影（不是另一层学习），称为"层级化"略有拔高。critique #27 的推荐是"高层选块、低层选门"的真分层，草稿没做到这一层却用了"层级化"措辞。

**判定：OK（这份草稿的诚实度高于一般论文修订稿，主动承认弱点；仅"层级化"一词轻微拔高）。**

---

## (2) 缺失点清单

1. **【serious 漏项】替代表示全景未给**（critique 维度2 #13）：草稿只给"onset 位掩码"一条修复路径，完全没讨论"只存 |ψ_t⟩=U_t|0⟩ 而非全 U_t（2048× 压缩，n=8→512 维）"和"stabilizer-rank 表示"和"XOR-AND/ESOP 图表示"。这些在"如果不完全对齐 SSHR bit-flip oracle、想保留酉矩阵视角"的退路下是关键备选。**建议 §3.2 末尾补一段"为何选 onset 掩码而非 |ψ_t⟩/stabilizer/XAG"的对比**。

2. **【serious 半覆盖】NPN 归一化作为搜索空间剪枝未提**（critique 维度3 #18 推荐(1)）：草稿 §5 只把 NPN 用作"训练数据扩增"，但 critique 明确推荐用 NPN 等价类**归一化搜索空间**（把搜索限制在代表元上再还原），这是更强的剪枝手段。**建议 §5 关键设计决策补一条"NPN 归一化搜索"**。

3. **【serious 半覆盖】候选枚举 vs 门序列深度的区分不够清晰**（critique 维度2 #11）：草稿把"609441 结构化候选（含几何）"和"112 原子动作（无几何）"的对比合并进了"动作换块"论证，没有单独点明"new-idea 是把 O(候选) 几何问题换成了 O(前传×深度) 学习问题，而网络又训不动"这个本质错配。

4. **【路线图】Phase 2 缺可证伪增益阈值**：Phase 1 有"≥ SSHR-I 95%"硬门槛，Phase 2（n=7 核心实验）只有"证明学习带来增益"的软描述。**建议补"n=7 CNOT 较 SSHR-Beam 降低 ≥ X%（如 5-10%）才算 RL 增量 claim 成立"**。

5. **【路线图】ShortCircuit 复现保真度验证未写**："适配量子度量"会引入偏差，需先在经典域复现 ShortCircuit 报告的数字再迁移，否则对比不公平。

6. **【诚实度轻微】§5 "层级化"措辞 vs 实际单层架构**：critique #27 的真分层（高层选块/低层选门）未实现，建议要么改名为"几何先验引导的 AlphaZero"（去掉"层级化"），要么补一个真正的分层设计。

7. **【minor】entropy bonus 缺失**：§2.7 提到"缺 entropy bonus"但 §5 修正版架构里**没把它加回去**（标准 AlphaZero loss 应有 c·H[π]）。修复清单不完整。

---

## (3) 如果只能补一处，补哪里

**补 #1：在 §3.2 末尾加一段"状态表示的备选方案对比"**——即 onset 位掩码 vs |ψ_t⟩(2^n 复向量) vs stabilizer-rank vs XOR-AND/ESOP 图，给出维度、是否需模拟、是否置换等变、与仓库接口兼容性四列对比，并说明为何选 onset 掩码。

**理由**：
- 它是**唯一一条被完全漏掉的 serious finding**（其余缺失都是半覆盖或路线图细节）。
- 它直接决定 §5 修正版架构的**网络输入层**设计——目前 §5 直接拍板"onset int + GNN cand_h"，但如果审稿人问"为什么不保留酉矩阵视角用 |ψ_t⟩=U_t|0⟩（这是 phase oracle 综合的天然表示）"，草稿现在答不上来。
- 它同时回应用户最关心的 **U_t 表示爆炸** 和 **|T> 定义** 两大要点：因为 |ψ_t⟩ 表示正好是"不彻底放弃酉视角、但把维度从 2×2^n×2^n 压到 2×2^n"的中间方案，讨论它能让 §3.1 弃用 |T⟩ 的论证更稳健（证明 author 不是只会"全弃用"，而是比较过中间方案）。
- 补这一处的工作量最小（一段对比表），但把草稿从"serious 漏 1 条"补成"serious 全覆盖"，是杠杆最高的单点修复。