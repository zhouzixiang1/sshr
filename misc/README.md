# Local miscellaneous assets

该目录保存不进入干净竞赛仓库的本机资产：

- `archive/`：历史项目、第三方源码/构建树、旧论文与恢复材料；
- `tmp/`：预览、渲染和临时工作文件；
- `generated/`：可再生成但当前保留的异常或历史构建产物。

除本 README 外，上述目录均由根 `.gitignore` 排除。不要把其中的二进制、私有
材料或旧结果复制进 `docs/competition/submission/`。恢复规则见
`../docs/archive/ARCHIVE_INDEX.md`。
